blog: {
  title: string
  slug: slug
  posts: [{
    title: string
    date: date
    slug: slug
    image: blob
    content: string
    abstract: string
    more: string
    images: [{
      image: blob
      description: string
      operations: [{
        command: string
      }]
    }]
  }]
}
